# Gemini API Key for OmniScan Titan X Document Intelligence
GEMINI_API_KEY=

# OPTIONAL: Remote AI Background Removal Service
# The application runs fully offline with the built-in Local Biometric AI engine by default.
# The following environment variables are optional and can be added on the server later
# if connecting a dedicated remote AI service (e.g. BiRefNet, RMBG, or custom Python backend):
# BG_REMOVAL_API_KEY=
# BG_REMOVAL_AUTH_HEADER=Bearer
# BG_REMOVAL_ENDPOINT=http://localhost:5000/api/background/remove

